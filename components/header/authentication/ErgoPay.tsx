import React from 'react';

// import { authApiGateway } from '@/api/auth';
import Button from '@/components/button/Button';

export const ErgoPay = () => {
  return (
    <Button fullWidth kind="Tonal" className="w-full rounded-s-2xl">
      Connect ErgoPay
    </Button>
  );
};
